name: bexlogix

services:
  osrm:
    image: ${OSRM_DOCKER_IMAGE:-osrm/osrm-backend:latest}
    container_name: bexlogix-osrm
    command: >
      osrm-routed
      --algorithm mld
      --max-table-size 1000
      --max-trip-size 1000
      /data/tehran-latest.osrm
    volumes:
      - ../../offline/osrm/data:/data:ro
    ports:
      - "127.0.0.1:5000:5000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "osrm-routed", "--help"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s

  vroom:
    image: ${VROOM_DOCKER_IMAGE:-ghcr.io/vroom-project/vroom-docker:v1.15.0-rc.1}
    container_name: bexlogix-vroom
    environment:
      - VROOM_ROUTER=osrm
    volumes:
      - ./vroom-conf:/conf
    depends_on:
      osrm:
        condition: service_started
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS 'http://127.0.0.1:3000/health' >/dev/null || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s

  tiles:
    image: ${TILE_DOCKER_IMAGE:-maptiler/tileserver-gl:latest}
    container_name: bexlogix-tiles
    volumes:
      - ../../offline/tiles/data:/data:ro
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider 'http://127.0.0.1:8080/styles.json' || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s
